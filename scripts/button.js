function runaway(id) {
    id.style.top = Math.round(Math.random() * 500) - 100 + 'px';
    id.style.left = Math.round(Math.random() * 550) - 200 + 'px';
    id.style.right = Math.round(Math.random() * 500) - 200 + 'px';
}